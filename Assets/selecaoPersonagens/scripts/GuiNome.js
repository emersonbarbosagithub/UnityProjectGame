/*
static var NomeJogador1 : String = "Jog1";
private var keyboard : TouchScreenKeyboard;
var TextField: Rect;
// Opens native keyboard optimized for URL entry
function OnGUI() {
    if (GUI.Button(TextField, NomeJogador1))
        keyboard = TouchScreenKeyboard.Open(NomeJogador1, TouchScreenKeyboardType.URL);

    if (keyboard)
        NomeJogador1 = keyboard.text;
    }
    
    */